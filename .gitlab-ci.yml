image: docker

services:
  - docker:dind

stages:
  - test
  - deploy

test:
  stage: test
  script:
    echo run tests here

deploy:
  stage: deploy
  script:
    - sudo apt-get install -y python-pip
    - pip install docker-compose
    - docker-compose up -d build
